# WoW 골드 분석기 웹 퍼블리싱 계획서

## 목차
1. [프로젝트 개요](#1-프로젝트-개요)
2. [현재 상태 분석](#2-현재-상태-분석)
3. [배포 옵션 비교](#3-배포-옵션-비교)
4. [권장 배포 전략](#4-권장-배포-전략)
5. [집 컴퓨터 서버 구축 방안](#5-집-컴퓨터-서버-구축-방안)
6. [데이터베이스 관리 계획](#6-데이터베이스-관리-계획)
7. [보안 관리 계획](#7-보안-관리-계획)
8. [무료 대안 옵션](#8-무료-대안-옵션)
9. [구현 로드맵](#9-구현-로드맵)
10. [비용 분석](#10-비용-분석)

---

## 1. 프로젝트 개요

### 프로젝트 정보
- **프로젝트명**: WoW 골드 거래 분석기 Enhanced
- **기술 스택**: Python, Gradio, SQLite, Plotly, Pandas
- **주요 기능**:
  - 골드 거래 데이터 파싱 및 저장
  - 시계열 분석 및 시각화
  - 가성비 순위 분석
  - GPC(Gold Per Coin) 분석
  - 최적 판매 전략 제안

### 현재 실행 방식
- 로컬 환경에서 `uv run` 또는 배치 파일로 실행
- Gradio 기본 포트: 7860
- 데이터베이스: SQLite (gold_trades.db)

---

## 2. 현재 상태 분석

### 장점
✅ Python 기반으로 배포가 상대적으로 간단
✅ Gradio 프레임워크로 웹 인터페이스 이미 구현됨
✅ SQLite 사용으로 별도 DB 서버 불필요
✅ 파일 기반 데이터 입력/저장 구조

### 고려사항
⚠️ 현재 로컬 환경에만 최적화됨
⚠️ 인증/권한 시스템 미구현
⚠️ HTTPS 미적용
⚠️ 동시 접속자 처리 미검증
⚠️ 자동 백업 시스템 부재

---

## 3. 배포 옵션 비교

### 옵션 A: 집 컴퓨터를 직접 웹 서버로 활용 ⭐ (가장 경제적)

**장점**
- 💰 **완전 무료** (전기료 제외)
- 🎮 하드웨어 성능 100% 활용
- 🗄️ 무제한 스토리지 (로컬 HDD/SSD)
- 🔧 완전한 제어권

**단점**
- 🏠 고정 IP 필요 또는 DDNS 사용
- ⚡ 전기료 부담 (24시간 가동 시)
- 🔒 보안 관리 책임
- 🌐 ISP의 업로드 속도 제한
- 💻 시스템 유지보수 필요

**권장 대상**: 개인/소규모 사용, 학습 목적

---

### 옵션 B: 클라우드 무료 플랫폼 활용 ⭐⭐ (가장 간편)

#### B-1. Hugging Face Spaces (추천 🏆)
```
무료 제공 사항:
- CPU: 2 vCore
- RAM: 16GB
- Storage: Persistent (무제한)
- 대역폭: 무제한
- 도메인: https://[username].hf.space
```

**장점**
- ✅ 완전 무료
- ✅ Git 기반 자동 배포
- ✅ Gradio 네이티브 지원
- ✅ HTTPS 자동 제공
- ✅ 글로벌 CDN

**단점**
- ❌ 30분 비활성 시 슬립 모드
- ❌ 커스텀 도메인 제한적
- ❌ SQLite 영구 저장 가능하나 Git 관리 필요

**비용**: 완전 무료

---

#### B-2. Railway.app
```
무료 제공 사항:
- 월 5달러 크레딧 (500시간 실행 가능)
- 512MB RAM
- 1GB Storage
```

**장점**
- ✅ GitHub 자동 배포
- ✅ 커스텀 도메인 무료
- ✅ 환경변수 관리 편리

**단점**
- ❌ 무료 크레딧 제한
- ❌ 리소스 제한적

---

#### B-3. Render.com
```
무료 제공 사항:
- 750시간/월
- 512MB RAM
- 비활성 시 슬립 모드
```

**장점**
- ✅ 자동 HTTPS
- ✅ 쉬운 배포

**단점**
- ❌ 슬립 모드 (15분 비활성)
- ❌ 콜드 스타트 느림

---

### 옵션 C: 저비용 클라우드 VPS ⭐⭐⭐ (가장 안정적)

#### C-1. Oracle Cloud Free Tier (최강 추천 🏆🏆🏆)
```
평생 무료 제공:
- VM: 2x AMD CPU (1/8 OCPU)
- RAM: 1GB x 2개 = 총 2GB
- Storage: 200GB Block Volume
- 트래픽: 10TB/월 아웃바운드
- 고정 IP: 무료
```

**장점**
- ✅ **평생 무료**
- ✅ 완전한 루트 권한
- ✅ 고정 IP 제공
- ✅ 24/7 가동
- ✅ 리소스 충분

**단점**
- ⚠️ 초기 설정 복잡
- ⚠️ 장기 미사용 시 계정 회수 가능

---

#### C-2. AWS Lightsail
```
최저가 플랜: $3.5/월
- 512MB RAM
- 1 vCore
- 20GB SSD
- 1TB 트래픽
```

---

#### C-3. DigitalOcean
```
최저가 플랜: $4/월
- 512MB RAM
- 1 vCore
- 10GB SSD
```

---

### 옵션 D: 하이브리드 (집 컴퓨터 + 클라우드)

**구성**
- 집 컴퓨터: 데이터 처리 및 DB 관리
- 클라우드: 프록시 서버 (보안 게이트웨이)

**장점**
- 집 IP 노출 방지
- 부하 분산 가능
- DDoS 보호

**단점**
- 복잡한 구성
- 지연 시간 증가 가능

---

## 4. 권장 배포 전략

### 🥇 1순위: Oracle Cloud Free Tier (개인 프로젝트 최적)

**이유**
1. **완전 무료**: 평생 무료로 사용 가능
2. **충분한 리소스**: RAM 2GB, 200GB 스토리지
3. **안정성**: 24/7 가동, 고정 IP
4. **제어권**: 완전한 루트 권한으로 자유로운 구성
5. **보안**: 집 IP 노출 없음

**단점**
- 초기 설정이 다소 복잡 (하지만 한번만 하면 됨)

---

### 🥈 2순위: Hugging Face Spaces (테스트/데모용)

**이유**
1. **완전 무료**
2. **배포 초간단**: Git push만으로 배포
3. **Gradio 네이티브**: 코드 수정 최소화

**단점**
- 30분 슬립 모드 (사용성 저하)
- SQLite 데이터 Git 관리 필요

**적합한 경우**
- 빠른 프로토타이핑
- 포트폴리오/데모 목적
- 간헐적 사용

---

### 🥉 3순위: 집 컴퓨터 직접 서버 (학습 목적)

**이유**
1. **무료** (전기료 제외)
2. **학습 기회**: 서버 관리, 보안, 네트워킹 학습
3. **무제한 리소스**

**단점**
- 보안 위험 (집 네트워크 노출)
- 전기료, 소음, 발열
- ISP 정책 제한 가능

**적합한 경우**
- 개인 사용 전용
- 서버 관리 학습 목적
- 외부 공개 불필요

---

## 5. 집 컴퓨터 서버 구축 방안

> ⚠️ 주의: 이 옵션을 선택할 경우, 보안에 각별히 주의해야 합니다.

### 5.1 네트워크 구성

#### A. 고정 IP vs DDNS

**고정 IP (권장하지 않음)**
- 비용: 월 10,000~30,000원 (ISP마다 다름)
- 장점: 안정적인 접속
- 단점: 추가 비용, IP 노출 위험

**DDNS (권장) 🏆**
- 비용: 무료
- 서비스: No-IP, DuckDNS, FreeDNS
- 작동 방식: 동적 IP를 도메인에 자동 매핑

```bash
# DuckDNS 예시
# 1. https://www.duckdns.org 가입
# 2. 도메인 생성 (예: mygoldanalyzer.duckdns.org)
# 3. 자동 업데이트 스크립트 설정

# Windows 작업 스케줄러로 5분마다 실행
echo url="https://www.duckdns.org/update?domains=mygoldanalyzer&token=YOUR_TOKEN&ip=" | curl -k -o duck.log -K -
```

---

#### B. 포트 포워딩 설정

**공유기 설정**
1. 공유기 관리자 페이지 접속 (보통 192.168.0.1 또는 192.168.1.1)
2. 포트 포워딩 설정:
   - 외부 포트: 443 (HTTPS)
   - 내부 포트: 7860 (Gradio)
   - 내부 IP: 서버 PC의 로컬 IP

**방화벽 설정 (Windows)**
```powershell
# PowerShell 관리자 권한 실행
New-NetFirewallRule -DisplayName "Gradio Server" -Direction Inbound -LocalPort 7860 -Protocol TCP -Action Allow
```

---

### 5.2 서버 소프트웨어 구성

#### A. Reverse Proxy (Nginx) - 필수 🔒

**왜 필요한가?**
- HTTPS 적용 (보안)
- 로드 밸런싱
- 정적 파일 캐싱
- DDoS 방어

**설치 (Windows)**
```bash
# Nginx for Windows 다운로드
# https://nginx.org/en/download.html

# 또는 Docker 사용 (권장)
docker run -d -p 80:80 -p 443:443 \
  -v c:/nginx/conf:/etc/nginx/conf.d \
  -v c:/nginx/certs:/etc/nginx/certs \
  nginx:latest
```

**설정 파일 (nginx.conf)**
```nginx
server {
    listen 80;
    server_name mygoldanalyzer.duckdns.org;

    # HTTP를 HTTPS로 리다이렉트
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl;
    server_name mygoldanalyzer.duckdns.org;

    ssl_certificate /etc/nginx/certs/cert.pem;
    ssl_certificate_key /etc/nginx/certs/key.pem;

    # 보안 헤더
    add_header Strict-Transport-Security "max-age=31536000" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;

    # Rate Limiting (DDoS 방어)
    limit_req_zone $binary_remote_addr zone=one:10m rate=10r/s;
    limit_req zone=one burst=20 nodelay;

    location / {
        proxy_pass http://localhost:7860;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

---

#### B. SSL 인증서 (무료)

**Let's Encrypt (권장) 🏆**

```bash
# Certbot 설치 (Windows)
# 1. https://certbot.eff.org/ 에서 다운로드
# 2. 인증서 발급

certbot certonly --standalone -d mygoldanalyzer.duckdns.org
```

**또는 Cloudflare (더 쉬움) 🏆🏆🏆**
1. Cloudflare 무료 계정 생성
2. DDNS 도메인을 Cloudflare DNS로 변경
3. SSL/TLS: "Full" 설정
4. 자동 HTTPS 리다이렉트 활성화
5. DDoS 보호 무료 제공

---

#### C. 프로세스 관리 (자동 재시작)

**방법 1: Windows 서비스 등록**

```python
# service_wrapper.py
import win32serviceutil
import win32service
import win32event
import subprocess

class GoldAnalyzerService(win32serviceutil.ServiceFramework):
    _svc_name_ = "GoldAnalyzerService"
    _svc_display_name_ = "WoW Gold Analyzer Service"

    def __init__(self, args):
        win32serviceutil.ServiceFramework.__init__(self, args)
        self.hWaitStop = win32event.CreateEvent(None, 0, 0, None)

    def SvcStop(self):
        self.ReportServiceStatus(win32service.SERVICE_STOP_PENDING)
        win32event.SetEvent(self.hWaitStop)

    def SvcDoRun(self):
        subprocess.call(['uv', 'run', 'gold_analyzer_enhanced.py'])

if __name__ == '__main__':
    win32serviceutil.HandleCommandLine(GoldAnalyzerService)
```

**방법 2: PM2 (Node.js 프로세스 매니저) - 권장**

```bash
# PM2 설치
npm install -g pm2 pm2-windows-startup

# PM2 자동 시작 설정
pm2-startup install

# 앱 등록
pm2 start "uv run gold_analyzer_enhanced.py" --name gold-analyzer
pm2 save
```

---

### 5.3 모니터링 및 유지보수

#### A. 로그 관리

```python
# 애플리케이션에 로깅 추가
import logging
from logging.handlers import RotatingFileHandler

# 로그 설정
handler = RotatingFileHandler('gold_analyzer.log', maxBytes=10*1024*1024, backupCount=5)
logging.basicConfig(
    handlers=[handler],
    level=logging.INFO,
    format='%(asctime)s %(levelname)s: %(message)s'
)
```

#### B. 시스템 모니터링 (무료 도구)

**Grafana + Prometheus**
- 시스템 리소스 모니터링
- 네트워크 트래픽 추적
- 알림 설정 가능

**UptimeRobot (무료)**
- 서비스 다운 감지
- 이메일/SMS 알림
- 50개 모니터 무료

---

## 6. 데이터베이스 관리 계획

### 6.1 현재 상태: SQLite

**장점**
- ✅ 설정 불필요
- ✅ 백업 간단 (파일 복사)
- ✅ 경량

**단점**
- ❌ 동시 쓰기 제한
- ❌ 대용량 데이터 처리 느림
- ❌ 네트워크 접근 불가

**권장 사항**: 현재 프로젝트 규모에 적합, 유지 권장

---

### 6.2 백업 전략 🔒

#### A. 자동 백업 스크립트

```python
# backup_db.py
import shutil
import os
from datetime import datetime

def backup_database():
    source = "gold_trades.db"
    timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")

    # 로컬 백업
    local_backup = f"backups/gold_trades_{timestamp}.db"
    os.makedirs("backups", exist_ok=True)
    shutil.copy2(source, local_backup)

    # 오래된 백업 삭제 (30일 이상)
    import glob
    import time
    now = time.time()
    for backup_file in glob.glob("backups/*.db"):
        if os.stat(backup_file).st_mtime < now - 30 * 86400:
            os.remove(backup_file)

    print(f"Backup created: {local_backup}")

if __name__ == "__main__":
    backup_database()
```

**Windows 작업 스케줄러로 매일 자동 실행**

---

#### B. 클라우드 백업 (무료 옵션)

**Google Drive (15GB 무료)**
```python
# pip install pydrive2

from pydrive2.auth import GoogleAuth
from pydrive2.drive import GoogleDrive

def upload_to_drive(file_path):
    gauth = GoogleAuth()
    gauth.LocalWebserverAuth()
    drive = GoogleDrive(gauth)

    file = drive.CreateFile({'title': os.path.basename(file_path)})
    file.SetContentFile(file_path)
    file.Upload()
```

**GitHub (Private Repo)**
```bash
# .gitignore에서 *.db 제거 (비공개 repo만!)
git add backups/*.db
git commit -m "Daily backup"
git push
```

---

### 6.3 PostgreSQL 마이그레이션 (선택사항)

**언제 필요한가?**
- 동시 접속자 100명 이상
- 복잡한 쿼리 필요
- 데이터 무결성 중요

**무료 호스팅 옵션**
1. **Supabase** (500MB 무료)
2. **ElephantSQL** (20MB 무료)
3. **Neon** (10GB 무료)

**마이그레이션 코드**
```python
# SQLite → PostgreSQL 변환 스크립트 (필요 시)
import sqlite3
import psycopg2

# 추후 필요 시 작성
```

---

## 7. 보안 관리 계획

### 7.1 필수 보안 조치 🔒

#### A. 인증 시스템 추가

**Gradio 기본 인증**
```python
# gold_analyzer_enhanced.py 수정

app.launch(
    share=False,
    inbrowser=True,
    server_port=7860,
    auth=("admin", "your_secure_password_here"),  # 기본 인증 추가
    auth_message="WoW 골드 분석기 로그인"
)
```

**또는 OAuth 통합 (더 안전)**
```python
# pip install gradio-oauth

import gradio as gr
from gradio_oauth import OAuth

oauth = OAuth(
    google_client_id="YOUR_CLIENT_ID",
    google_client_secret="YOUR_SECRET"
)

app.launch(auth=oauth)
```

---

#### B. Rate Limiting (속도 제한)

```python
# pip install slowapi

from slowapi import Limiter
from slowapi.util import get_remote_address

limiter = Limiter(key_func=get_remote_address)

@limiter.limit("10/minute")
def process_text_input(text):
    # 기존 코드
    pass
```

---

#### C. 입력 검증 (SQLi, XSS 방지)

```python
import re
import html

def sanitize_input(text):
    # HTML 이스케이프
    text = html.escape(text)

    # 최대 길이 제한
    if len(text) > 100000:
        raise ValueError("입력이 너무 깁니다")

    # 위험한 패턴 차단
    dangerous_patterns = [
        r'<script', r'javascript:', r'onerror=',
        r'DROP TABLE', r'DELETE FROM'
    ]
    for pattern in dangerous_patterns:
        if re.search(pattern, text, re.IGNORECASE):
            raise ValueError("허용되지 않는 입력입니다")

    return text

# 사용
def process_text_input(analyzer, text_input):
    text_input = sanitize_input(text_input)
    # 기존 처리 로직
```

---

#### D. 환경 변수로 민감 정보 관리

```python
# .env 파일 생성 (Git에 추가 금지!)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_secure_password
DB_ENCRYPTION_KEY=random_32_byte_key

# 코드에서 사용
import os
from dotenv import load_dotenv

load_dotenv()

admin_user = os.getenv("ADMIN_USERNAME")
admin_pass = os.getenv("ADMIN_PASSWORD")
```

---

### 7.2 네트워크 보안

#### A. 방화벽 설정

**Windows Defender Firewall**
```powershell
# 필요한 포트만 허용
New-NetFirewallRule -DisplayName "HTTPS" -Direction Inbound -LocalPort 443 -Protocol TCP -Action Allow
New-NetFirewallRule -DisplayName "HTTP" -Direction Inbound -LocalPort 80 -Protocol TCP -Action Allow

# 기타 모든 포트 차단
Set-NetFirewallProfile -DefaultInboundAction Block
```

**Cloudflare Firewall (권장)**
- 알려진 봇 차단
- 국가별 접근 제어
- Challenge 페이지 설정

---

#### B. Fail2Ban (무단 접근 차단)

```bash
# Linux 서버에서 사용 가능
# Windows는 IP Ban Middleware로 대체

# Python 미들웨어 예시
failed_attempts = {}

def check_auth(ip):
    if ip in failed_attempts and failed_attempts[ip] > 5:
        raise Exception("Too many failed attempts")
```

---

### 7.3 시스템 보안

#### A. 자동 업데이트

```bash
# Windows Update 자동화
# 설정 > Windows Update > 고급 옵션 > 자동 업데이트 활성화

# Python 패키지 업데이트
pip install --upgrade pip
pip list --outdated
pip install --upgrade gradio pandas plotly
```

#### B. 안티바이러스 / 안티멀웨어

**무료 옵션**
- Windows Defender (기본 내장, 충분함)
- Malwarebytes Free

#### C. 정기 보안 점검 체크리스트

```
□ 비밀번호 변경 (3개월마다)
□ 로그 파일 검토 (주간)
□ 백업 검증 (월간)
□ 사용하지 않는 포트 닫기
□ SSL 인증서 갱신 확인
□ 의심스러운 트래픽 확인
□ 시스템 업데이트 적용
```

---

## 8. 무료 대안 옵션

### 8.1 완전 무료 솔루션 종합 비교표

| 플랫폼 | 비용 | RAM | Storage | 트래픽 | 슬립모드 | HTTPS | 추천도 |
|--------|------|-----|---------|--------|----------|-------|--------|
| **Oracle Cloud** | 무료(평생) | 2GB | 200GB | 10TB/월 | 없음 | ✅ | ⭐⭐⭐⭐⭐ |
| **Hugging Face** | 무료 | 16GB | 무제한 | 무제한 | 30분 | ✅ | ⭐⭐⭐⭐ |
| **Render** | 무료 | 512MB | 1GB | 100GB/월 | 15분 | ✅ | ⭐⭐⭐ |
| **Railway** | $5/월 크레딧 | 512MB | 1GB | 제한 | 없음 | ✅ | ⭐⭐⭐ |
| **Google Cloud** | $300 크레딧 | 설정가능 | 설정가능 | 설정가능 | 없음 | ✅ | ⭐⭐⭐ |
| **집 컴퓨터** | 전기료만 | 무제한 | 무제한 | ISP제한 | 없음 | 설정필요 | ⭐⭐⭐⭐ |

---

### 8.2 각 옵션별 상세 가이드

#### 🏆 Oracle Cloud Free Tier 설정 가이드

**1단계: 계정 생성**
```
1. https://www.oracle.com/cloud/free/ 접속
2. "Start for free" 클릭
3. 신용카드 등록 (인증용, 청구 안됨)
4. 무료 티어 선택 (Always Free)
```

**2단계: VM 인스턴스 생성**
```
1. Compute > Instances > Create Instance
2. Image: Ubuntu 22.04 (권장)
3. Shape: VM.Standard.E2.1.Micro (Always Free)
4. Boot Volume: 50GB
5. VCN: 자동 생성 또는 기존 VCN 사용
6. Assign Public IP: 활성화
7. SSH Key: 생성 및 다운로드
```

**3단계: 방화벽 설정**
```bash
# Oracle Cloud Console에서
# Networking > Virtual Cloud Networks > Security Lists
# Ingress Rules 추가:
Source: 0.0.0.0/0
Protocol: TCP
Port: 80, 443, 7860
```

**4단계: 서버 설정**
```bash
# SSH 접속
ssh -i your-key.pem ubuntu@your-instance-ip

# 시스템 업데이트
sudo apt update && sudo apt upgrade -y

# Python 및 의존성 설치
sudo apt install python3-pip python3-venv nginx -y

# 프로젝트 업로드 (Git 사용)
git clone https://github.com/your-repo/gold_analysis.git
cd gold_analysis

# 가상환경 생성
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# PM2로 프로세스 관리
sudo npm install -g pm2
pm2 start "python gold_analyzer_enhanced.py" --name gold-analyzer
pm2 startup
pm2 save
```

**5단계: Nginx 리버스 프록시**
```nginx
# /etc/nginx/sites-available/gold-analyzer
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:7860;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
    }
}

# 심볼릭 링크 생성
sudo ln -s /etc/nginx/sites-available/gold-analyzer /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

**6단계: SSL 인증서 (Let's Encrypt)**
```bash
sudo apt install certbot python3-certbot-nginx -y
sudo certbot --nginx -d your-domain.com
```

---

#### 🥈 Hugging Face Spaces 설정 가이드

**1단계: 저장소 준비**
```bash
# 새 디렉토리 생성
mkdir hf-gold-analyzer
cd hf-gold-analyzer

# requirements.txt 생성
cat > requirements.txt << EOF
gradio
pandas
matplotlib
plotly
seaborn
EOF

# app.py 생성 (파일명 필수: app.py)
cp gold_analyzer_enhanced.py app.py

# app.py 수정: 마지막 줄 변경
# app.launch(share=False, inbrowser=True, server_port=7860)
# →
# app.launch()
```

**2단계: Hugging Face 업로드**
```bash
# Git LFS 설치 (대용량 파일용)
git lfs install

# HF CLI 설치
pip install huggingface_hub

# 로그인
huggingface-cli login

# 저장소 생성 및 푸시
git init
git add .
git commit -m "Initial commit"
git remote add origin https://huggingface.co/spaces/YOUR_USERNAME/gold-analyzer
git push -u origin main
```

**3단계: Space 설정**
```yaml
# README.md에 메타데이터 추가 (파일 맨 위에)
---
title: WoW Gold Analyzer
emoji: 💰
colorFrom: yellow
colorTo: orange
sdk: gradio
sdk_version: 5.47.2
app_file: app.py
pinned: false
---
```

**SQLite 영구 저장 방법**
```python
# app.py에 추가
import os

# HF Spaces는 /data 디렉토리만 영구 저장
DB_PATH = "/data/gold_trades.db" if os.path.exists("/data") else "gold_trades.db"

# 데이터베이스 초기화 시 경로 사용
db = GoldTradeDatabase(db_path=DB_PATH)
```

---

#### 🥉 집 컴퓨터 + Cloudflare Tunnel (IP 숨기기)

**Cloudflare Tunnel의 장점**
- ✅ 포트 포워딩 불필요
- ✅ 집 IP 노출 안됨
- ✅ 자동 HTTPS
- ✅ DDoS 보호

**설정 방법**

```bash
# 1. Cloudflared 다운로드 (Windows)
# https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/install-and-setup/installation/

# 2. 인증
cloudflared tunnel login

# 3. 터널 생성
cloudflared tunnel create gold-analyzer

# 4. 설정 파일 생성 (config.yml)
cat > config.yml << EOF
tunnel: TUNNEL_ID
credentials-file: C:\Users\mrson\.cloudflared\TUNNEL_ID.json

ingress:
  - hostname: gold.yourdomain.com
    service: http://localhost:7860
  - service: http_status:404
EOF

# 5. DNS 라우팅 설정
cloudflared tunnel route dns gold-analyzer gold.yourdomain.com

# 6. 터널 실행
cloudflared tunnel run gold-analyzer

# 7. Windows 서비스로 등록 (자동 시작)
cloudflared service install
```

---

### 8.3 도메인 무료 옵션

| 서비스 | 형식 | 비용 | 추천도 |
|--------|------|------|--------|
| **Freenom** | .tk, .ml, .ga | 무료 | ⭐⭐ (신뢰도 낮음) |
| **DuckDNS** | yourname.duckdns.org | 무료 | ⭐⭐⭐⭐ |
| **No-IP** | yourname.no-ip.org | 무료 | ⭐⭐⭐ |
| **GitHub Pages** | username.github.io | 무료 | ⭐⭐⭐ (정적만) |
| **Cloudflare Pages** | yourname.pages.dev | 무료 | ⭐⭐⭐⭐ |

**저렴한 유료 도메인**
- Namecheap: $0.99/년 (첫해) .com
- Google Domains: $12/년 .com
- Cloudflare Registrar: 원가 (마크업 없음)

---

## 9. 구현 로드맵

### Phase 1: 로컬 테스트 및 준비 (1-2일)

#### Day 1: 보안 강화
```
□ 입력 검증 추가 (sanitize_input)
□ 기본 인증 추가 (Gradio auth)
□ 환경 변수 설정 (.env)
□ 로깅 시스템 추가
□ 에러 처리 개선
```

#### Day 2: 백업 및 모니터링
```
□ 자동 백업 스크립트 작성
□ 백업 테스트 (복구 확인)
□ 로그 rotation 설정
□ 헬스체크 엔드포인트 추가
```

---

### Phase 2: 배포 (2-3일)

#### 옵션 A: Oracle Cloud 배포
```
Day 1:
□ Oracle Cloud 계정 생성
□ VM 인스턴스 생성
□ 방화벽 규칙 설정

Day 2:
□ 서버 환경 설정 (Python, Nginx)
□ 프로젝트 업로드
□ 서비스 실행 확인

Day 3:
□ SSL 인증서 설치
□ 도메인 연결
□ 최종 테스트
```

#### 옵션 B: Hugging Face Spaces 배포
```
Day 1:
□ HF 계정 생성
□ 저장소 준비 (app.py, requirements.txt)
□ README.md 메타데이터 추가
□ Git 푸시
□ 배포 확인
```

#### 옵션 C: 집 컴퓨터 배포
```
Day 1:
□ DDNS 설정 (DuckDNS)
□ 포트 포워딩 또는 Cloudflare Tunnel
□ Nginx 설치 및 설정

Day 2:
□ SSL 인증서 (Let's Encrypt 또는 Cloudflare)
□ 방화벽 설정
□ PM2 프로세스 관리

Day 3:
□ 보안 점검
□ 백업 자동화 테스트
□ 모니터링 설정
```

---

### Phase 3: 모니터링 및 최적화 (진행 중)

#### Week 1: 안정화
```
□ 일일 로그 확인
□ 성능 모니터링
□ 사용자 피드백 수집
□ 버그 수정
```

#### Month 1: 최적화
```
□ 데이터베이스 쿼리 최적화
□ 캐싱 추가 (Redis 검토)
□ UI/UX 개선
□ 문서화
```

#### Month 3: 확장성 검토
```
□ 동시 접속자 테스트
□ PostgreSQL 마이그레이션 검토
□ CDN 도입 검토
□ API 개발 검토
```

---

## 10. 비용 분석

### 10.1 완전 무료 옵션 비교

#### 옵션 1: Oracle Cloud Free Tier ✅
```
초기 비용: $0
월 비용: $0 (평생 무료)
연 비용: $0

추가 비용:
- 도메인 (선택): $12/년 (.com)
- 총 비용: $0~12/년
```

#### 옵션 2: Hugging Face Spaces ✅
```
초기 비용: $0
월 비용: $0
연 비용: $0

제약사항:
- 30분 슬립 모드
- 기업용 부적합

총 비용: $0/년
```

#### 옵션 3: 집 컴퓨터 ✅
```
초기 비용:
- Nginx/Let's Encrypt: $0
- Cloudflare Tunnel: $0

월 비용:
- 전기료: 약 10,000~20,000원/월 (24시간 가동 시)
  * 소비 전력: 50W (저전력 PC)
  * 전기료: 50W × 24h × 30일 × 200원/kWh = 7,200원/월

선택 비용:
- 고정 IP: 10,000~30,000원/월 (선택, DDNS 사용 시 불필요)
- DDNS: $0 (DuckDNS, No-IP)

연 비용: 86,400~360,000원/년 (전기료만 vs 전기료+고정IP)
```

---

### 10.2 유료 옵션 비교 (참고용)

#### DigitalOcean Droplet
```
월 비용: $4~12
연 비용: $48~144
장점: 안정적, 간단한 관리
```

#### AWS Lightsail
```
월 비용: $3.5~10
연 비용: $42~120
장점: AWS 생태계 연동
```

#### Heroku (더 이상 무료 없음)
```
월 비용: $7~25
연 비용: $84~300
```

---

### 10.3 권장 조합

#### 🏆 최고의 무료 조합
```
서버: Oracle Cloud Free Tier ($0/년)
도메인: DuckDNS ($0/년)
SSL: Let's Encrypt ($0/년)
백업: Google Drive ($0/년, 15GB)
모니터링: UptimeRobot ($0/년)

총 비용: $0/년
```

#### 🥈 프로페셔널 조합 (저비용)
```
서버: Oracle Cloud Free Tier ($0/년)
도메인: Namecheap ($12/년)
CDN/보안: Cloudflare Free ($0/년)
백업: Backblaze B2 ($0.005/GB/월, ~$1/년)
모니터링: Datadog Free ($0/년)

총 비용: ~$13/년
```

#### 🥉 하이브리드 (학습 + 안정성)
```
개발/테스트: 집 컴퓨터 + Cloudflare Tunnel
프로덕션: Oracle Cloud Free Tier
백업: 양쪽 동기화

총 비용: ~$10/년 (전기료)
```

---

## 11. 최종 권장사항

### 🎯 용도별 추천

#### 1. 개인 학습 목적
**추천**: 집 컴퓨터 + Cloudflare Tunnel
- 이유: 서버 관리 경험 습득, 무료
- 구성: 집 PC → Cloudflare Tunnel → 인터넷
- 비용: 전기료만

#### 2. 포트폴리오/데모
**추천**: Hugging Face Spaces
- 이유: 배포 초간단, 완전 무료, 공유 링크
- 비용: $0
- 제약: 30분 슬립 모드 (데모용으로는 충분)

#### 3. 실제 서비스 운영
**추천**: Oracle Cloud Free Tier
- 이유: 안정적, 24/7 가동, 충분한 리소스
- 비용: $0 (평생 무료)
- 보너스: 실제 클라우드 경험

#### 4. 최고 안정성 필요
**추천**: Oracle Cloud + Cloudflare
- 이유: 무료이면서 엔터프라이즈급 보안
- 구성:
  - Oracle Cloud: 컴퓨팅
  - Cloudflare: CDN, DDoS 보호, WAF
- 비용: $0

---

### ⚠️ 주의사항

1. **집 컴퓨터 사용 시**:
   - ISP 약관 확인 (서버 운영 금지 조항 있을 수 있음)
   - 업로드 속도 확인 (보통 10~50Mbps)
   - 보안에 각별히 주의

2. **무료 클라우드 사용 시**:
   - 사용 약관 숙지 (상업적 이용 제한 확인)
   - 리소스 제한 (CPU, RAM, 트래픽)
   - 데이터 백업 필수

3. **SQLite 사용 시**:
   - 동시 접속자 수 제한 (보통 100명 이하 권장)
   - 정기 백업 필수
   - 파일 손상 대비 체크섬 검증

---

## 12. 체크리스트

### 배포 전 점검 ✅

```
보안:
□ 기본 인증 또는 OAuth 추가
□ 입력 검증 (XSS, SQLi 방지)
□ HTTPS 적용
□ 방화벽 설정
□ Rate limiting 적용
□ 환경 변수로 비밀 정보 관리

백업:
□ 자동 백업 스크립트 작성
□ 백업 복구 테스트
□ 오프사이트 백업 (클라우드)
□ 백업 주기 설정 (일/주/월)

모니터링:
□ 로그 시스템 설정
□ 업타임 모니터링 (UptimeRobot 등)
□ 에러 알림 설정
□ 리소스 모니터링 (CPU, RAM, 디스크)

성능:
□ 캐싱 전략 (필요 시)
□ 데이터베이스 인덱스 확인
□ 정적 파일 CDN (필요 시)
□ 이미지 최적화

법적/윤리:
□ 개인정보 처리방침 (필요 시)
□ 이용약관 (필요 시)
□ 저작권 확인 (WoW 관련)
□ 사용자 데이터 보호 (GDPR 등)
```

---

## 13. 다음 단계

### 즉시 실행 가능한 작업

1. **보안 강화 (우선순위 1)**
   ```bash
   # 1. 기본 인증 추가
   # 2. 입력 검증 추가
   # 3. .env 파일 생성
   ```

2. **백업 시스템 구축 (우선순위 2)**
   ```bash
   # 1. backup_db.py 작성
   # 2. Windows 작업 스케줄러 설정
   # 3. 백업 복구 테스트
   ```

3. **배포 플랫폼 선택 (우선순위 3)**
   ```
   권장: Oracle Cloud Free Tier
   대안: Hugging Face Spaces (빠른 데모)
   ```

---

## 14. 추가 자료

### 학습 자료
- [Gradio 공식 문서](https://gradio.app/docs/)
- [Oracle Cloud 무료 티어 가이드](https://docs.oracle.com/en-us/iaas/Content/FreeTier/freetier.htm)
- [Nginx 리버스 프록시 설정](https://nginx.org/en/docs/)
- [Let's Encrypt 인증서 발급](https://letsencrypt.org/getting-started/)
- [Cloudflare Tunnel 가이드](https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/)

### 커뮤니티
- [Gradio Discord](https://discord.gg/gradio)
- [Oracle Cloud 한국 커뮤니티](https://cafe.naver.com/oraclecloud)

---

## 15. 결론

**이 프로젝트의 최적 배포 전략:**

1. **단기 (1주일)**:
   - Hugging Face Spaces에 배포하여 빠르게 테스트
   - 비용: $0
   - 시간: 1시간

2. **중기 (1개월)**:
   - Oracle Cloud Free Tier로 이전
   - Cloudflare와 연동하여 보안 강화
   - 비용: $0 (도메인 제외)
   - 시간: 1일

3. **장기 (3개월+)**:
   - 사용자 증가에 따라 PostgreSQL 고려
   - CDN 도입 (Cloudflare 무료)
   - API 개발 (모바일 앱 등)

**총 예상 비용: $0~12/년** (도메인만 선택적으로 구매)

**핵심 메시지**: 무료 옵션만으로도 충분히 프로페셔널한 서비스를 운영할 수 있습니다!

---

**문서 버전**: 1.0
**작성일**: 2026-01-09
**다음 리뷰**: 배포 후 1개월

---

## 부록 A: 빠른 시작 명령어 모음

### Oracle Cloud 배포 (복사-붙여넣기)
```bash
# SSH 접속 후
sudo apt update && sudo apt upgrade -y
sudo apt install python3-pip python3-venv nginx git -y
git clone YOUR_REPO_URL
cd gold_analysis
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
sudo npm install -g pm2
pm2 start "python gold_analyzer_enhanced.py" --name gold-analyzer
pm2 startup
pm2 save
sudo apt install certbot python3-certbot-nginx -y
sudo certbot --nginx -d your-domain.com
```

### Hugging Face Spaces 배포
```bash
# 로컬에서
cp gold_analyzer_enhanced.py app.py
# app.launch() 수정
git init
git add .
git commit -m "Initial commit"
git remote add origin https://huggingface.co/spaces/USERNAME/SPACENAME
git push -u origin main
```

### 집 컴퓨터 + Cloudflare Tunnel
```bash
# Windows PowerShell
# 1. Cloudflared 다운로드 및 설치
cloudflared tunnel login
cloudflared tunnel create gold-analyzer
# config.yml 작성
cloudflared tunnel route dns gold-analyzer gold.yourdomain.com
cloudflared service install
```

---

이 문서가 프로젝트 배포에 도움이 되기를 바랍니다! 🚀
